# 2026-05-07

- Heartbeat caught broken `openclaw-autosync.service`: repeated failures were caused by a stale restic repository lock from PID 2708 created on 2026-05-06 17:02:45.
- Verified no live `restic`/`rclone` process owned that lock, ran `restic unlock`, then manually reran `backup/run-restic-backup.sh` successfully and cleared the failed systemd state.
- Heartbeat caught failing `openclaw-local-backup.service`: local fallback backups were accidentally backing up `~/.openclaw/local-backups` into themselves, which caused restic warnings and a failed unit state.
- Fixed `backup/restic-excludes.txt` to exclude `~/.openclaw/local-backups`, reran `backup/run-local-restic-backup.sh` successfully, and cleared the failed systemd state.
