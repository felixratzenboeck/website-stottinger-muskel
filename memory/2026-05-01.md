# 2026-05-01

- Philips TV im Thema "Fernseher" identifiziert und gekoppelt: Modell 55OLED903/12, IP 192.168.0.131, Philips API v6.
- Direkte API-Steuerung für Kontrast und Videokontrast eingerichtet.
- Lokales Tool `/home/leo/.openclaw/workspace/tools/philips_tv_control.py` angelegt für Power, Apps, TV/Sender und Kontrast.
- Wichtige Synonyme vorbereitet: TV/Fernseher an/aus, YouTube, DAZN/The Zone, Netflix, Spotify, ORF1/ORF2/ORF3.
- Ambilight-Steuerung ergänzt: an/aus, Video folgen, Audio folgen, Farbe folgen sowie Farb-Synonyme für Hot Lava/Deep Water/Fresh Nature/Warm White/Cool White.
- Skill angelegt: `skills/philips-tv-control/SKILL.md` mit Referenz-Cheatsheet und lokalem Steuer-Tool für künftige Sessions.
- Felix wants a free, robust backup setup for all OpenClaw data: Google Drive + Restic as the main encrypted backup, with private GitHub in addition for workspace/text/history.
- Local prep was reportedly completed earlier in-session (restic/rclone/git/sqlite3, backup scripts, restore path, timer files, local git repo), but the setup is not complete until the rclone authorize token is provided as plain text and Google Drive is actually connected.
- Felix prefers Brave as the default browser; when browser attach tried Chrome, he explicitly corrected this.
- Philips-TV-Tool angepasst: Beim Befehl zum Ausschalten wird Ambilight jetzt ebenfalls explizit ausgeschaltet, damit es nicht von selbst weiterleuchtet.
- Onboard-Audio auf dem Debian-PC nach BIOS-Aktivierung erkannt (Intel HDA / ALC887-VD Analog). AirPlay-Empfänger `Leo Boxen` mit `shairport-sync` + `avahi-daemon` eingerichtet; Hilfsskript `tools/airplay_boxen.sh` angelegt.
- Tailscale-interne TV-Shortcut-API + Weboberfläche für iPhone/Mac eingerichtet (`leo-tv-shortcuts.service` auf `100.125.38.70:8766`) mit Endpunkten für TV an/aus sowie Kontrast und Videokontrast.
- Current blocker: the long rclone authorize token was shown in screenshots but not yet provided as copyable text; screenshots were not trusted enough for exact token extraction.
- Backup stack finalized: Google Drive now uses a dedicated free Google OAuth client, the remote restic repo was reorganized to `OpenClaw-Backup/restic-repo`, readable overview/docs/logs/restore folders are exported alongside it, and a private GitHub repo `felixratzenboeck/openclaw-workspace` is live.
- Automation finalized: `openclaw-autosync.timer` runs every 30 minutes to commit/push workspace changes and refresh the cloud backup + Drive overview, while `openclaw-local-backup.timer` remains as the 6-hour local fallback.
